{
  "name": "@recubed/async",
  "version": "0.0.1",
  "description": "Functional async primitives",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "tanfonto <tanfonto@pm.me>",
  "license": "MIT",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "lint": "eslint -c .eslintrc.js src/**/*.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "tap --100 tests/**/*.test.ts",
    "test:dev": "tap --100 tests/**/*.test.ts",
    "posttest": "tap --coverage-report=text-lcov | coveralls",
    "ct": "tap --watch --changed --100"
  },
  "devDependencies": {
    "@types/delay": "^3.1.0",
    "@typescript-eslint/eslint-plugin": "^2.0.0",
    "@typescript-eslint/parser": "^2.0.0",
    "coveralls": "^3.0.6",
    "delay": "^4.3.0",
    "eslint": "^6.0.1",
    "range": "^0.0.3",
    "tap": "^14.6.1",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "@recubed/async": "file:../task/recubed-async-v0.0.1.tgz",
    "global": "^4.4.0",
    "vmd": "^1.34.0"
  }
}
